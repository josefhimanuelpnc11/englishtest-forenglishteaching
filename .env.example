# Google Apps Script Web app URL (/exec) from
# Deploy > New deployment > Web app.
# Leave empty to run in local-only mode (results are
# kept in the browser localStorage as backup).
VITE_GAS_ENDPOINT=

# Optional shared secret — must match SHARED_SECRET
# in apps-script/Code.gs. Leave empty if disabled.
VITE_GAS_SECRET=
